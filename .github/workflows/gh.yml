name: gh

on:
  push:
  workflow_dispatch:

jobs:
  gh:
    runs-on: ubuntu-latest
    steps:
      - name: test
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api /app
