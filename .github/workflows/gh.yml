name: gh

on:
  push:
  workflow_dispatch:

jobs:
  gh:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          (
            gh api /installation/repositories || echo $?
            gh api /user || echo $?
          ) >> $GITHUB_STEP_SUMMARY
